export interface NavItem {
  label: string;
  href: string;
}

export interface Problem {
  title: string;
  description: string;
}

export interface Ingredient {
  name: string;
  benefit: string;
  detail: string;
}
